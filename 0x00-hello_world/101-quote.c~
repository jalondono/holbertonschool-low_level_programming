#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int write(int filedes, const char *buf, unsigned int nbyte);
write(1, "Hello World!\n", 13);
return (1);
}
