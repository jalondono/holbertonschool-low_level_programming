#include "holberton.h"

/**
 * add - check the code for Holberton School students.
 * @a : return  de sum of to ints
 * @b : return so
 * Return: Always 0.
 */
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
