/**
 *op_add - print a name throught a pointer
 *@a: string to copy on new buffer
 *@b: pointer to function
 *Result: a matrix with H character
 *Return: a pointer if can do  the buffer
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 *op_sub - print a name throught a pointer
 *@a: string to copy on new buffer
 *@b: pointer to function
 *Result: a matrix with H character
 *Return: a pointer if can do  the buffer
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 *op_mul - print a name throught a pointer
 *@a: string to copy on new buffer
 *@b: pointer to function
 *Result: a matrix with H character
 *Return: a pointer if can do  the buffer
 */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 *op_div - print a name throught a pointer
 *@a: string to copy on new buffer
 *@b: pointer to function
 *Result: a matrix with H character
 *Return: a pointer if can do  the buffer
 */
int op_div(int a, int b)
{
	return (a / b);
}
/**
 *op_mod - print a name throught a pointer
 *@a: string to copy on new buffer
 *@b: pointer to function
 *Result: a matrix with H character
 *Return: a pointer if can do  the buffer
 */
int op_mod(int a, int b)
{
	return (a % b);
}
